1. Next.js란?
- React 기반의 프레임워크로, 서버 사이드 렌더링(SSR), 정적 사이트 생성(SSG), API 라우트등을 지원
- 성능 최적화의 개발 편의성을 높이는 것이 특징

2. Next.js의 역사
- Next.js는 2016년 Vercel(구 Zeit)에서 개발
- React는 컴포넌트 기반 UI 개발에는 탁월했지만, SEO 최적화와 초기 로딩 속도 문제를 해결하는데 문제가 있어 이를 보완하기 위해 Next.js 가 등장

3. Next.js의 주요 기능

- 라우팅 시스템
> 파일 기반 라우팅을 제공
/pages/index.js -> 경로
/page/about.js -> about 경로
/page/blog/[id].js -> 동적 라우팅 (/blog/123)

- 랜더링 방식
> 다양한 랜더링 방식을 지원
> 서버 사이드 랜더링 (SSR) -> 요청 시 서버에서 페이지 생성
> 정적 사이트 생성 (SSG) -> 빌드 시 HTML 미리 생성
> 클라이언트 사이드 랜더링 -> 프론트엔드에서 데이터 가져오기
> 점진적 정적 생성(ISR) -> SSG처럼 미리 생성하면서, 일정 시간마다 갱신

- API Routes
> 백엔드 없이 /pages/api/ ㄷ레거리에 API 엔드포인트 생성 기능
> EXpress.js 같은 별도 서버 필요 없이 API 개발 가능

- 이미지 최적화
> next/image 컴포넌트를 사용해 자동 이미지 최적화

- 국제화(i18n)
> 다국어 지원 기능 내장

- Webpack & SWC
> Webpack으로 
> SWC 사용 시시 빌드 속도 증가

4. Next.js의 장섬
- SEO 최적화
- 빠른 성능
- 개발 편의성
- 강력한 데이터 패칭
- 이미지 최적화
- 대규모 애플리케이션 개발 가능

5. Next.js의 단점
- 학습 곡선(러닝 커브)가 존재(기본적인 React 사용법 외에도 SSR, SSG, ISR 개념을 익혀야 함)
- 서버 비용 증가 가능(SSR을 과도하게 사용하면 서버 부하가 커지고 비용 증가 가능)
- 빌드 속도 이슈(SSG 페이지가 많을 경우 빌드 시간이 길어질 수 있음)
- 백엔드 기능 부족(API Routes 기능이 있지만, 본격적인 백엔드 서버 기능은 부족)

6. Next.js를 사용하는 유명기업
- Vercel(Next.js 개발사)
- Netflix
- TikTok
- Twitch
- Github Copilot
---------------------------------------
npx란?
- Node.js 8.2.0 이상부터 기본적으로 포함된 도구로, 패키지를 실행하는 용도로 사용.
- npx를 활용하면 특정 패키지를 실행하기 위해 전역 설치 없이도 사용할 수 있음

1. npx 특징
- 전역 설치 없이 패키지 실행
> 필요한 패키지를 일시적으로 다운로드한 후 실행하고, 이후 해당 패키지를 삭제
> 일회성 실행이 필요한 경우 매우 유용하다.
> e.g. npx cowsay "Hello, npx!"
> cowsay 패지키를 임시로 다운로드하여 실행한 후 제거

- 프로젝트 로컬패키지 실행
> node_modules/.bin 내부의 패키지를 찾아 실행
> 전역 설치 없이 프로젝트 내에서만 설치된 패키지를 실행하는 것이 가능
> npm install eslint --save-dev
> npx exlint .

- 특정 버전의 패키지 실행
> npx를 이용하면 특정 버전의 패키지를 설치하지 않고 실행
> npx typescript@4.5 --version

- 설치된 패키지가 없는 경우 설치 후 실행
> npx create-react-app my-app

2. npx 사용 시 주의할 점
- 반복적으로 사용할 경우 설치하는 것이 더 효율적.
- npx는 임시로 다운로드 후 실행하는 방식
- 자주 실행하는 패키지는 전역 또는 로컬로 설치하는 것이 더 효율적.
- npx는 원격에서 패키지를 다운로드해서 실행함으로, 신뢰할 수 있는 패키지만 실행하는 것이 좋음.
- npx는 실행한 패키지를 캐시에 저장하여 동일한 패키지를 다시 실행할 경우 빠르게 실행 됨.
- npx는 npm과 함께 제공되는 강력한 도구로 패키지를 실행하는 용도로 사용함

---------------------------------------
1. 페이지 라우터

Next 초창기부터 제공되어 오던 구 버전 라우터
많은 기업에서 사용되고 있는 안정적인 라우터
React Router 처럼 페이지 라우팅 기능 제공

※ create-next-app : Next.js 프로젝트를 쉽게 생성하는 공식 CLI 도구구
npx create-next-app@14 next-page-router


2. 앱 라우터